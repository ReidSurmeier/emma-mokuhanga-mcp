# Issue tracker

Use GitHub Issues in `ReidSurmeier/emma-mokuhanga-mcp`.

An implementation issue should define one vertical behavior slice, its
observable acceptance criteria, the tests that establish it, and any domain
or safety documentation that must change. Keep physical-print validation
separate from software-only completion.
