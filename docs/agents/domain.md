# Domain documentation

`CONTEXT.md` is the canonical glossary and invariant list for this
single-context repository. Accepted architectural decisions live in
`docs/adr/`.

Update `CONTEXT.md` when code introduces or changes a durable domain term.
Add an ADR when a decision changes project identity, output authority,
runtime exposure, persistence ownership, or the boundary between software
evidence and physical-print evidence.
